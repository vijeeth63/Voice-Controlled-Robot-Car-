# Public HTTPS URL of the FastAPI bridge (ngrok or other)
VITE_API_URL=https://your-subdomain.ngrok-free.app

# Must match BRIDGE_API_KEY on the server (treat as secret; anyone with this key can drive the bot)
VITE_API_KEY=change-me-to-a-long-random-string
